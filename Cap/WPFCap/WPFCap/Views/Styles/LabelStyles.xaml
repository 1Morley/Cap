<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource DefaultLabelTextColor}"/>
        <Setter Property="FontSize" Value="{DynamicResource DefaultLabelFontSize}"/>
    </Style>

    <Style TargetType="Label" x:Key="Title" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="Foreground" Value="{DynamicResource TitleTextColor}"/>
        <Setter Property="FontSize" Value="{DynamicResource TitleFontSize}"/>
    </Style>
    
    <Style TargetType="Label" x:Key="SubTitle" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="Foreground" Value="{DynamicResource SubTitleTextColor}"/>
        <Setter Property="FontSize" Value="{DynamicResource SubTitleFontSize}"/>
    </Style>
</ResourceDictionary>