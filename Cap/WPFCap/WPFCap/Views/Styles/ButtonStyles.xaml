<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button">

        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
        <Setter Property="Background" Value="{DynamicResource ButtonBackgroundColor}"/>
        <Setter Property="Foreground" Value="{DynamicResource ButtonTextColor}"/>
        <Setter Property="BorderThickness" Value="{DynamicResource ElementBorderThickness}"/>

        <Setter Property="Padding" Value="{DynamicResource ButtonPadding}"/>
        <Setter Property="FontWeight" Value="{DynamicResource ButtonBoldWeight}"/>
        <Setter Property="FontSize" Value="{DynamicResource ElementFontSize}"/>




        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="{DynamicResource ButtonCornerCurve}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding Content}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource FocusButtonBorderColor}"/>
                <Setter Property="Background" Value="{DynamicResource FocusButtonBackgroundColor}"/>
                <Setter Property="Foreground" Value="{DynamicResource FocusButtonTextColor}"/>
            </Trigger>
        </Style.Triggers>

    </Style>

    <Style TargetType="Button" x:Key="EqualSides" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Width" Value="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight}"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <Style TargetType="Button" x:Key="WindowFunctions" BasedOn="{StaticResource EqualSides}">
        <Setter Property="Margin" Value="{DynamicResource WindowFunctionMargin}"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Content" Value="X"/>
    </Style>


    
</ResourceDictionary>