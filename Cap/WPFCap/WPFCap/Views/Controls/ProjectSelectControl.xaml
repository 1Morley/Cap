<UserControl x:Class="WPFCap.Views.Controls.ProjectSelectControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mini="clr-namespace:WPFCap.Views.MiniControls"
             xmlns:model="clr-namespace:WPFCap.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ItemsControl ItemsSource="{Binding ShownProjectList}" 
                      AlternationCount="4"
                      Margin="20,0"
                      ItemTemplate="{StaticResource ListProjectModelTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <Button Content="-"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Style="{DynamicResource EqualSides}"
                Command="{Binding SwitchDeleteModeCommand}"/>
        <mini:PageSwitchControl/>
    </Grid>
</UserControl>
