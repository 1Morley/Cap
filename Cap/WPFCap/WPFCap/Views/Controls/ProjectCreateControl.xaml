<UserControl x:Class="WPFCap.Views.Controls.ProjectCreateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mini="clr-namespace:WPFCap.Views.MiniControls">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <mini:PictureSelectControl Grid.Column="0"
                                 DataContext="{Binding SelectPictureVM}"/>

        <StackPanel Grid.Column="2">
            <Label Content="CREATE PROJECT"/>
            <TextBox Text="{Binding InputProject.Title}"/>
            <TextBox Text="Style Select"/>

        </StackPanel>
        <Button  Grid.Column="2" 
                 HorizontalAlignment="Right" 
                 VerticalAlignment="Bottom" 
                 Command="{Binding AddProjectCommand}"
                 Content="Submit"/>

    </Grid>
</UserControl>
