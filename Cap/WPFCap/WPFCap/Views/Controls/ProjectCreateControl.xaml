<UserControl x:Class="WPFCap.Views.Controls.ProjectCreateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFCap.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!--<mini:PictureSelectControl Grid.Column="0"
                                 DataContext="{Binding SelectPictureVM}"/>-->
        <local:ImageModelSelectControl Grid.Column="0"
                                       DataContext="{Binding InputProjectVM.ImageListVM}"/>

        <StackPanel Grid.Column="2" DataContext="{Binding InputProjectVM}">
            <Label Content="CREATE PROJECT"/>
            <TextBox Text="{Binding InputModel.Title}"/>

        </StackPanel>
        <Button  Grid.Column="2" 
                 HorizontalAlignment="Right" 
                 VerticalAlignment="Bottom" 
                 Command="{Binding AddProject}"
                 Content="Submit"/>
    </Grid>
</UserControl>
