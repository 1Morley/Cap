<UserControl x:Class="WpfCapstone.Views.FocusCreateEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:WpfCapstone.Views"
             mc:Ignorable="d" >
    <Grid Background="CadetBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <views:FocusBaseControl 
                Grid.Row="0" 
                Grid.ColumnSpan="2"/>
        <StackPanel Grid.Column="0"
                    Grid.Row="1" >
            <Label Content="Title:"/>
            <Label Content="Status:"/>
            <Label Content="Due Date:"/>
            <Label Content="Notes:"/>
        </StackPanel>
        <StackPanel Grid.Column="1"
                    Grid.Row="1" >
            <TextBox Text="{Binding EntryListVM.InputEntry.Title}"/>
            <TextBox Text="{Binding EntryListVM.InputEntry.Status}"/>
            <DatePicker SelectedDate="{Binding EntryListVM.InputEntry.DueDate}"/>
            <TextBox Text="{Binding EntryListVM.InputEntry.EntryNotes}"/>
        </StackPanel>
        <Button Grid.Row="2" 
                Grid.ColumnSpan="2" 
                Content="submit" 
                Width="80"
                Height="30"
                Command="{Binding ClickCreateEntry}"/>
    </Grid>
</UserControl>
